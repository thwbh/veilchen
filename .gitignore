node_modules

# We migrated from npm to deno for dependency resolution.
# deno.lock is the source of truth; package-lock.json is ignored on
# purpose so it never comes back via stray `npm install` calls.
package-lock.json

# Output
/build
/dist

# svelte-package scratch dir
/.svelte-kit

# IDEA
.idea/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
